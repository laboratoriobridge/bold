export interface ZIndex {
    modalBackdrop: number
    modalContainer: number
    popper: number
    dropdown: number
    tooltip: number
}

export const zIndex: ZIndex = {
    modalBackdrop: 1050,
    modalContainer: 1100,
    popper: 1200,
    dropdown: 1300,
    tooltip: 1400,
}
