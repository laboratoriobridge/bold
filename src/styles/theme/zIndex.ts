export interface ZIndex {
    modalBackdrop: number
    modalContainer: number
    overlays: number
}

export const zIndex: ZIndex = {
    modalBackdrop: 1030,
    modalContainer: 1040,
    overlays: 1050,
}
