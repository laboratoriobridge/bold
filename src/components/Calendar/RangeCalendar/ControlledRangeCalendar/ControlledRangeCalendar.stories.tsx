import { storiesOf } from '@storybook/react'
import React from 'react'

import { ControlledRangeCalendar } from './ControlledRangeCalendar'

storiesOf('Components|Controlled Range Calendar', module).add('undefined interval', () => <ControlledRangeCalendar />)
