import { KeyNavigationDirection } from '../Droppable/types/model'

export const keyDirectionMap: Record<string, KeyNavigationDirection> = {
  ArrowRight: 'right',
  ArrowLeft: 'left',
  ArrowUp: 'up',
  ArrowDown: 'down',
}
